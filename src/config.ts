//Amount of tokens to mint in each request
export const minting_batch_size:number = 200;

//Delay in (ms) between each minting request
export const minting_batch_delay:number = 0;

//Delay in (ms) between each minting request
export const minting_request_delay:number = 5000;